JSON:

{
  "id": 1,
  "quote": "Life isn't about getting and having, it's about giving and being.",
  "author": "Kevin Kruse"
}

Saída:

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Quote Object",
  "description": "Schema for a single quote object.",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "Unique identifier for the quote."
    },
    "quote": {
      "type": "string",
      "description": "The text of the quote.",
      "minLength": 1  // Ensures the quote is not empty.
    },
    "author": {
      "type": "string",
      "description": "The author of the quote."
    }
  },
  "required": [
    "id",
    "quote",
    "author"
  ]
}

