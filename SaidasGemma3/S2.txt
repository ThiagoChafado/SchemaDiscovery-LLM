JSON:
{ "name"   : "Alice Brown",
  "sku"    : "54321",
  "price"  : 199.95,
  "shipTo" : { "name" : "Bob Brown",
               "address" : "456 Oak Lane",
               "city" : "Pretendville",
               "state" : "HI",
               "zip"   : "98999" },
  "billTo" : { "name" : "Alice Brown",
               "address" : "456 Oak Lane",
               "city" : "Pretendville",
               "state" : "HI",
               "zip"   : "98999" }
}

Saída:


{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CustomerOrder",
  "description": "Schema for a customer order with shipping and billing addresses.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The customer’s full name."
    },
    "sku": {
      "type": "string",
      "description": "Stock Keeping Unit, a unique identifier for the product."
    },
    "price": {
      "type": "number",
      "format": "float",
      "description": "The price of the item."
    },
    "shipTo": {
      "type": "object",
      "description": "Shipping address information.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Recipient’s name for shipping."
        },
        "address": {
          "type": "string",
          "description": "Street Address."
        },
        "city": {
          "type": "string",
          "description": "City of the shipping address."
        },
        "state": {
          "type": "string",
          "description": "State abbreviation (e.g., 'HI').",
          "enum": ["HI", "CA", "TX", /* Add other valid state abbreviations here */]
        },
        "zip": {
          "type": "string",
          "description": "Zip code.",
          "pattern": "^\\d{5}$"  // Example: Matches 5-digit zip codes. Adjust as needed.
        }
      },
      "required": ["name", "address", "city", "state", "zip"]
    },
    "billTo": {
      "type": "object",
      "description": "Billing address information.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Recipient’s name for billing."
        },
        "address": {
          "type": "string",
          "description": "Street Address."
        },
        "city": {
          "type": "string",
          "description": "City of the billing address."
        },
        "state": {
          "type": "string",
          "description": "State abbreviation (e.g., 'HI').",
          "enum": ["HI", "CA", "TX", /* Add other valid state abbreviations here */]
        },
        "zip": {
          "type": "string",
          "description": "Zip code.",
          "pattern": "^\\d{5}$"  // Example: Matches 5-digit zip codes. Adjust as needed.
        }
      },
      "required": ["name", "address", "city", "state", "zip"]
    }
  },
  "required": ["name", "sku", "price", "shipTo", "billTo"]
}
