JSON:

{
    "max_block_depth": 25,
    "project_dir": "test_dir",
    "skip_action_validation": false,
    "rules": {
        "deprecation": {
            "exclude_paths": []
        }
    }
}

Saída:

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConfigurationObject",
  "description": "Schema for a configuration object.",
  "type": "object",
  "properties": {
    "max_block_depth": {
      "type": "integer",
      "description": "The maximum block depth."
    },
    "project_dir": {
      "type": "string",
      "description": "The project directory path."
    },
    "skip_action_validation": {
      "type": "boolean",
      "description": "Whether to skip action validation."
    },
    "rules": {
      "type": "object",
      "description": "A collection of rules.",
      "properties": {
        "deprecation": {
          "type": "object",
          "description": "Rules related to deprecation.",
          "properties": {
            "exclude_paths": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Paths to exclude from deprecation rules."
            }
          },
          "required": [
            "exclude_paths"
          ]
        }
      }
    }
  },
  "required": [
    "max_block_depth",
    "project_dir",
    "skip_action_validation",
    "rules"
  ]
}
